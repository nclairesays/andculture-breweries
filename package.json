{
    "name": "andculture-breweries",
    "version": "0.1.0",
    "engines": {
        "node": "10.15.3",
        "npm": "6.10.1"
    },
    "scripts": {
        "build": "cd client && yarn install && yarn build && cd ..",
        "deploy": "cp -a client/build/. public/",
        "postinstall": "yarn run build && yarn run deploy && echo 'Client has been built!'"
    },
    "dependencies": {
        "yarn": "^1.17.3"
    }
}
